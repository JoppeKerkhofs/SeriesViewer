// this is the component for the settings page, here we will display a form to change the user's settings

const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
        </div>
    );
};

export default Settings;